# 2026-08-21

## link-nvidia Dockerfile 全面修复

### 根因
- `tar -zxf` 未指定 `-C /tmp/`，解压到 CWD(`/`)，但后续 `mv` 命令在 `/tmp/` 中查找 → amd64 构建失败
- amd64 tar 包含扁平 `sing-box` 文件；arm64 tar 包含 `sing-box-<ver>-linux-arm64/` 目录（含 sing-box + libcronet.so）
- arm64 的 `libcronet.so` 未被复制到镜像中，是 glibc 链接的，在 Alpine (musl) 上需要 `gcompat`

### 修复清单
1. **Dockerfile**: 添加 `-C /tmp/` 到 tar 解压命令；合并 4 个 RUN 为 1 个层；安装 `gcompat`；处理 arm64 libcronet.so；修复 shell `|| true` 优先级掩盖 mv 失败的 bug（用 `{ }` 分组）；合并 mkdir 到 entrypoint COPY 层；HEALTHCHECK start-period 5s→10s
2. **entrypoint.sh**: 二进制调用改绝对路径（/usr/sbin/rsyslogd2、/usr/local/bin/php-fpm、/usr/local/bin/subscriptiond）；移除死代码 SING_BOX_VERSION/CLOUDFLARED_VERSION；WARP 密钥默认值无条件设置（避免 WARP 禁用时 config 模板产生空值导致 sing-box 崩溃）
3. **docker-compose.yml**: 移除废弃 `version: '3.8'`；WARP_ENABLED 默认值 true→false（与 entrypoint 一致）
4. **config.yaml.template**: `reserved` 字段去掉引号（`${WARP_RESERVED}` → 生成数组而非字符串）
5. **GitHub Actions main.yml**: 移除未使用的 build-args（SING_BOX_VERSION/CLOUDFLARED_VERSION）和对应 env 变量（Dockerfile 无 ARG 声明，被静默忽略）

## 运行时第二处报错修复

### 报错
`decode config at /etc/apache2/config.json: dns.servers[0].address: json: unknown field "address"`

### 根因
sing-box 1.12+ 的 `type: "https"` DNS server 已无 `address` 字段（该字段属于已废弃的 legacy 格式，需 ENABLE_DEPRECATED_LEGACY_DNS_SERVERS 才生效）。config.yaml.template 中 google/alidns 两个 DoH 服务器误用了 `address`，严格 JSON 解析直接报错。

### 修复
- templates/config.yaml.template: 删除 google 和 alidns 两个 HTTPS DNS server 的 `"address": "..."` 字段（server 已包含服务器地址，地址多余且非法）
- 注意：`server` 使用域名 dns.google / dns.alidns.com，依赖 `detour: "direct"` 的 dialer 解析；若运行时 DoH 解析失败，可改为 IP+`tls.server_name` 模式（如 server: 8.8.8.8 + tls.server_name: dns.google）

## 统一 sing-box 版本声明为 1.13.19

用户要求：内置 sing-box 版本为 1.13.19，所有文档与代码必须明确修正。

### 修复清单
1. **README.md**: 简介「sing-box 1.11」→「sing-box 1.13.19」；docker-compose 示例移除废弃 `version: '3.8'`、`WARP_ENABLED` 默认值 true→false；环境变量表移除未接入构建的 `SING_BOX_VERSION`/`CLOUDFLARED_VERSION` 行（之前已从 workflow/entrypoint 删除，属误导）、`WARP_ENABLED` 默认值 true→false；新增「内置组件版本」提示块（sing-box 1.13.19、cloudflared 2026.8.2，不可通过环境变量覆盖）
2. **Dockerfile**: 顶部新增注释声明内置 sing-box 1.13.19 + cloudflared 2026.8.2；arm64 注释 `<version>`→`1.13.19`
3. **DEPLOY.md**: 概述区新增内置版本提示（sing-box 1.13.19 / cloudflared 2026.8.2）

### 关键事实
- 镜像版本由仓库 `bin/` 目录提交的二进制固定（amd64 扁平 `sing-box`、arm64 `sing-box-1.13.19-linux-arm64/`），不通过环境变量/构建参数覆盖；之前的 SING_BOX_VERSION/CLOUDFLARED_VERSION build-args 与 env 已确认是死代码并清除。

## 第三个坑：1.12+ 已移除 geosite，原方案「下载 geosite.db」无效

### 关键发现
- 用户原打算「下载 geo 库」解决 DNS 分流，但查证官方文档确认：**sing-box 1.12.0 起 GeoSite/GeoIP 数据库已被移除**，`geosite`/`geoip` 规则匹配器不再存在。1.13.19 属已移除区间。
- 因此 `config.yaml.template` 中 `dns.rules` 的 `"geosite": "cn"` / `"geosite": "geolocation-!cn"` 本身是**非法字段**，修掉 `address` 后会成为下一个 FATAL 报错；下载 `geosite.db` 也救不了（无 `route.geosite` 指向机制）。

### 修正方案（rule-set / .srs）
- **Dockerfile**: 构建时 `wget` 下载 `geosite-cn.srs` 与 `geosite-geolocation-%21cn.srs`（URL 中 `!` 需编码为 `%21`）到 `/usr/local/share/sing-box/`（本地文件名用 `!`，Dockerfile 内用 `'!'` 引号保护）。
- **config.yaml.template**: `route` 新增 `rule_set` 数组，定义 `geosite-cn`、`geosite-geolocation-!cn` 两个 `type: local / format: binary` 规则集；`dns.rules` 由 `geosite` 改为 `rule_set` 引用。
- **校验**: 用 Python 渲染模板（env 占位符替换为示例值）后 `json.loads` 通过；并断言「全局无 geosite/geoip 作为 KEY」「dns.servers 无 address」「dns.rules 全部指向已定义 rule_set」「rule_set 格式正确」——全部通过。
- **README.md**: 在「内置组件版本」提示块补充说明 1.12+ 移除 geosite、改用 rule-set 实现分流。

### 关键事实（新增）
- 1.13.19 的 DNS 地质分流只能用 rule-set（SRS），不能用旧版 geosite.db。
- SagerNet/sing-geosite 仓库 `rule-set` 分支提供按 category 的 `.srs` 文件（如 `geosite-cn.srs`、`geosite-geolocation-!cn.srs`），已验证 HTTP 200 可用。
- 沙箱可联网（curl 到 raw.githubusercontent.com 返回 200）。
